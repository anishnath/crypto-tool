import math

# Pi constant
print(f"Pi: {math.pi}")

# Calculate circle area
radius = 5
area = math.pi * radius ** 2
print(f"Circle area (r=5): {area}")

# Calculate circumference
circumference = 2 * math.pi * radius
print(f"Circumference (r=5): {circumference}")
