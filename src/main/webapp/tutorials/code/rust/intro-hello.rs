fn main() {
    println!("Hello, World!");
    println!("Welcome to Rust programming!");
    
    // Rust is a systems programming language
    // that runs blazingly fast, prevents segfaults,
    // and guarantees thread safety.
    
    let message = "Rust makes systems programming accessible!";
    println!("{}", message);
}
